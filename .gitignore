/target
__pycache__/
/target-musl-*

# Web frontend: node_modules is never committed; frontend/dist IS committed
# (small, hashed bundles) so plain `cargo build` / `cargo install` works
# without a Node toolchain.
node_modules/
